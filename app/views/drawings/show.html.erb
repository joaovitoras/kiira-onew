<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @drawing.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @drawing.description %>
</p>
<p>
  <strong>Image:</strong>
  <%= image_tag @drawing.image_url.to_s %>
</p>

<%= link_to 'Edit', edit_drawing_path(@drawing) %> |
<%= link_to 'Back', drawings_path %>
