  <div class="container">
<div class="row">
  <div class="col-lg-12">
      <h1 class="page-header" style="margin-top: 0px;">Produtos <i class="fa fa-send"></i></h1>
  </div>
</div>
</div>
<% @sale.products.each do |product| %>
  <div class="container">
      <div class="row">
          <div class="col-md-3">
              <div class="panel panel-danger">
                  <div class="panel-heading">
                      <h4 class="text-center" style="margin-top: 0px;margin-bottom: 0px;">
                          <%= product.name %></h4>
                  </div>
                  <%= image_tag(product.image_url(:home), class: "img-responsive", style: "border: none !important;" ) if product.image? %>
                  <div class="panel-body text-center" style="padding:0px">
                          <h5><%= product.description %></h5> 
                          <h4 class="text-success"><strong>R$ <%= product.price %></strong></h4>
                          
                  </div>
                  
                  </ul>
                  <div class="panel-footer">
                      <a class="btn btn-block btn-danger" href="http://www.jquery2dotnet.com">Entre em contato</a>
                  </div>
              </div>
          </div>
      </div>
  </div>
<% end %>